machine:
#  python:
#    version: 2.7.13
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild-27 || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild-27 /bin/sh urpms -g UnitedRPMs/opera -s opera.spec -r true -d 'dist .fc27':
        parallel: true
        timeout: 9600

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/8ff0b35066468a87de71

        


