machine:
#  python:
#    version: 2.7.13
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild-26 || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild-26 /bin/sh urpms -g UnitedRPMs/faad2 -s faad2.spec -r true -d 'dist .fc26' -c true:
        parallel: true
        timeout: 9600

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/8ff0b35066468a87de71

        


