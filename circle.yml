machine:
#  python:
#    version: 2.7.13
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild-25 || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild-25 /bin/sh urpms -g UnitedRPMs/x264 -s x264.spec -r true -d 'dist .fc25' -c true:
        parallel: true
        timeout: 9600

        


