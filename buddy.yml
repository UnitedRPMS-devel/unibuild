- pipeline: "production"
  trigger_mode: "MANUAL"
  ref_name: "master"
  actions:
  - action: "Execute: npm test"
    type: "BUILD"
    docker_image_name: "davidva/unibuild-28"
    docker_image_tag: "6"
    execute_commands:
    - "rm -f urpms"
    - "wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms"
    - "chmod a+x urpms"
    - "/urpms -g UnitedRPMs/libva-intel-driver -s libva-intel-driver.spec -r true -d 'dist .fc28'"

  
